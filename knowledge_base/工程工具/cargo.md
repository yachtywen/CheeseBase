# Cargo Notes

Cargo is Rust's package manager and build tool.

Common commands:

- `cargo build`
- `cargo run`
- `cargo test`
- `cargo fmt`
- `cargo clippy -- -D warnings`

Cargo manages dependencies through `Cargo.toml` and records exact dependency
versions in `Cargo.lock`.
