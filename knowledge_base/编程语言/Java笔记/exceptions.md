# Java Exception Notes

Java exceptions separate normal control flow from error handling.

Checked exceptions must be declared or caught, while unchecked exceptions
usually represent programming errors. In service code, exceptions should carry
clear context and should not be swallowed silently.

Useful keywords: exception, checked exception, runtime exception, try catch,
finally, error handling.
